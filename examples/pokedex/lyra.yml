version: 0.1

schema:
  num: string
  name: string
  img: string
  type: string
  height: string
  weight: string
  weaknesses: string

input:
  path: ./get-data.js

output:
  name: pokemon.js
  directory: tmp

deploy:
  platform: cloudflare
  configuration:
    workerName: pokedex
